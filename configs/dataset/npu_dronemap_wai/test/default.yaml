dataset_str:
  "NPUDroneMapWAI(
    split='${dataset.npu_dronemap_wai.test.split}',
    resolution=${dataset.npu_dronemap_wai.test.dataset_resolution},
    principal_point_centered=${dataset.npu_dronemap_wai.test.principal_point_centered},
    seed=${dataset.npu_dronemap_wai.test.seed},
    transform='${dataset.npu_dronemap_wai.test.transform}',
    data_norm_type='${dataset.npu_dronemap_wai.test.data_norm_type}',
    ROOT='${dataset.npu_dronemap_wai.test.ROOT}',
    dataset_metadata_dir='${dataset.npu_dronemap_wai.test.dataset_metadata_dir}',
    overfit_num_sets=${dataset.npu_dronemap_wai.test.overfit_num_sets},
    variable_num_views=${dataset.npu_dronemap_wai.test.variable_num_views},
    num_views=${dataset.npu_dronemap_wai.test.num_views},
    covisibility_thres=${dataset.npu_dronemap_wai.test.covisibility_thres},
    covisibility_thres_max=${dataset.npu_dronemap_wai.test.covisibility_thres_max},
    sampling_mode='${dataset.npu_dronemap_wai.test.sampling_mode}',
    load_modalities=${dataset.npu_dronemap_wai.test.load_modalities},)"
split: 'test'
dataset_resolution: ${dataset.resolution_test_npu_dronemap_wai}
principal_point_centered: ${dataset.principal_point_centered}
seed: 777
transform: 'imgnorm'
data_norm_type: ${model.data_norm_type}
ROOT: ${root_data_dir}/NPU_Dronemap
dataset_metadata_dir: ${mapanything_dataset_metadata_dir}
overfit_num_sets: null
variable_num_views: ${dataset.test.variable_num_views}
num_views: ${dataset.num_views}
covisibility_thres: ${dataset.covisibility_thres_min}
covisibility_thres_max: ${dataset.covisibility_thres_max}
sampling_mode: ${dataset.sampling_mode}
load_modalities: ["image", "depth"]
