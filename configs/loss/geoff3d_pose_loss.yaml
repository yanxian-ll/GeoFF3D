# GeoFF3D pose-focused loss with three parts:
#   1. Original Pi3/Pi3X view0-relative geometry loss for consistent reconstruction.
#   2. Extra true dataset-world-frame point loss for world coordinate alignment.
#   3. Explicit dataset-world-frame camera translation/rotation loss.
#
# This config is safe for variable_num_views with min_num_views=1. Pairwise
# relative pose loss in the original view0-relative geometry term is disabled
# because there are no relative pose pairs for single-view batches. Absolute
# world-frame pose supervision is provided by WorldFramePoseLoss below.

train_criterion: |
  ExcludeTopNPercentPixelLoss(
    FactoredGeometryRegr3DPlusNormalGMLoss(
      RobustRegressionLoss(alpha=0.5, scaling_c=0.05),
      norm_mode='avg_dis',
      depth_type_for_loss='depth_z',
      loss_in_log=True,
      flatten_across_image_only=True,
      compute_pairwise_relative_pose_loss=True,
      convert_predictions_to_view0_frame=True,
      compute_world_frame_points_loss=True,
      apply_normal_and_gm_loss_to_synthetic_data_only=True,
      normal_loss_weight=3.0,
      gm_loss_weight=3.0
    ),
    top_n_percent=5,
    apply_to_real_data_only=True,
    loss_set_indices=[0, 1, 2]
  ) + ExcludeTopNPercentPixelLoss(
    WorldFramePointsRegr3D(
      RobustRegressionLoss(alpha=0.5, scaling_c=0.05),
      norm_mode='avg_dis',
      loss_in_log=True,
      flatten_across_image_only=True,
      detach_world_frame_points_geometry=True,
      detach_world_frame_points_translation=False,
      world_frame_points_loss_weight=1.0
    ),
    top_n_percent=5,
    apply_to_real_data_only=True,
    loss_set_indices=[0]
  ) + WorldFramePoseLoss(
    FactoredLLoss(
      reduction='mean',
      pose_trans_loss_type='l1',
      pose_quats_loss_type='l1'
    ),
    norm_mode='avg_dis',
    depth_type_for_loss='depth_z',
    pose_trans_loss_weight=1.0,
    pose_quats_loss_weight=1.0,
    compute_absolute_pose_loss=True,
    compute_pairwise_relative_pose_loss=False
  )

test_criterion: |
  ExcludeTopNPercentPixelLoss(
    FactoredGeometryRegr3DPlusNormalGMLoss(
      RobustRegressionLoss(alpha=0.5, scaling_c=0.05),
      norm_mode='avg_dis',
      depth_type_for_loss='depth_z',
      loss_in_log=True,
      flatten_across_image_only=True,
      compute_pairwise_relative_pose_loss=True,
      convert_predictions_to_view0_frame=True,
      compute_world_frame_points_loss=True,
      apply_normal_and_gm_loss_to_synthetic_data_only=True,
      normal_loss_weight=3.0,
      gm_loss_weight=3.0
    ),
    top_n_percent=5,
    apply_to_real_data_only=True,
    loss_set_indices=[0, 1, 2]
  ) + ExcludeTopNPercentPixelLoss(
    WorldFramePointsRegr3D(
      RobustRegressionLoss(alpha=0.5, scaling_c=0.05),
      norm_mode='avg_dis',
      loss_in_log=True,
      flatten_across_image_only=True,
      detach_world_frame_points_geometry=True,
      detach_world_frame_points_translation=False,
      world_frame_points_loss_weight=1.0
    ),
    top_n_percent=5,
    apply_to_real_data_only=True,
    loss_set_indices=[0]
  ) + WorldFramePoseLoss(
    FactoredLLoss(
      reduction='mean',
      pose_trans_loss_type='l1',
      pose_quats_loss_type='l1'
    ),
    norm_mode='avg_dis',
    depth_type_for_loss='depth_z',
    pose_trans_loss_weight=1.0,
    pose_quats_loss_weight=1.0,
    compute_absolute_pose_loss=True,
    compute_pairwise_relative_pose_loss=False
  )