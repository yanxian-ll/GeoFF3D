
- [x] [DOM] render orthodom from chunks (现在分块不对，不应该扔掉Gaussian)
- [x] [gsplat] 研究gsplat的参数，具体是否有删除gaussian，添加Gaussian；
- [] [prior] 研究输入 prior （translation）：模拟添加噪声，模拟部分信号中断；


- [x] [大场景] 添加分块后的可视化效果，保存一张图像xy平面，绘制点，不同分块绘制core点为不同颜色；（占时不会只seam点，会很乱）
- [x] [大场景] 后优化部分在超大场景可能导致效果反而变差，我觉得后对齐不能只考虑点云（如果误差太大就保留原结果）；

- [x] 大场景重建也支持对输入先验做扰动，模拟误差；（待测试）
- [ ] 添加一个新指标，模型的z-up预测的准确性如何？对齐后和对齐前的一个z轴角度误差；感觉不太好衡量？
- [x] 在后对齐的时候，每两个节点是如何选重叠图像用于对齐的？如果分布不均匀会影响最终效果，例如大部分图像在同一个位置拍摄的，这导致这块位置采样到的点主导，导致对齐结果错误。  
- [ ] 分块也存在类似问题，有些区域在同一个地方拍摄过于密集，导致这部分分块只能看到非常局部的区域。一方面，理论上不需要这么多图像用于重建，降低了后续效率；一方面，导致前馈模型预测效果差。

- [x] 支持保存DSM，以及DSM可视化结果；
- [x] 支持等高线绘制；（待优化）

- [x] 修改脚本，将输出路径放到sh脚本中；添加新脚本，支持 pose perburb；添加新脚本，后对齐使用sim3；添加新脚本，不使用 prior propagation（包括ray和depth）；


- [x] 还是得支持通过confidence剔除点云才合理，而不是voxel
- [x] 可视化geoff3d的confidence，看一下是不是需要重新设计loss，重新训练。



